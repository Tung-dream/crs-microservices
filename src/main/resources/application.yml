server:
  port: 8082

spring:
  # Cấu hình kết nối Database
  datasource:
    url: jdbc:mysql://localhost:3306/course_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password:

  # Cấu hình JPA / Hibernate
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update